# Front-end environment variables (do NOT commit real secrets)
# Copy to `.env` inside `front-end/` and adjust values for your environment.

# Base URL of the NestJS API
API_BASE_URL=http://localhost:3000

# Optional: Sentry / Analytics DSNs, feature flags, etc.
# REACT_APP_SENTRY_DSN=
# REACT_APP_FEATURE_FLAGS=beta

